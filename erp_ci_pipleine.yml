trigger:
  branches:
    include:
      - develop
      - release/*

variables:
  - group: facility-erp-ci-vars
  - name: PLATFORM
    value: linux/amd64

stages:
- stage: CI
  displayName: CI – Branch Check, Versioning, Build & Push
  jobs:

  # ------------------------------------------------
  # Job 1: Branch & Version Check
  # ------------------------------------------------
  - job: Branch_Check
    displayName: 'Check Branch & Release Version'
    pool: 'hsn-shost-agent'

    steps:
      - bash: |
          set -e

          PROJECT="$(echo "$(Build.Repository.Name)" | awk -F'/' '{print $2}')"
          FULL_BRANCH="$(Build.SourceBranch)"
          BRANCH_NAME="$(Build.SourceBranchName)"

          echo "Triggered branch: $FULL_BRANCH"
          echo "Branch name: $BRANCH_NAME"
          echo "Expected release branch: $RELEASE_BRANCH"
          echo "Project name: $PROJECT"

          STOP_PIPELINE=false
          SKIP_CD=false
          IMAGE_VERSION=""

          # -------------------------------
          # Semantic version validator
          # -------------------------------
          validate_version() {
            [[ "$1" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]
          }

          # -------------------------------
          # Develop branch
          # -------------------------------
          if [[ "$BRANCH_NAME" == "develop" ]]; then
              echo "Develop branch detected"
              IMAGE_VERSION="$DEVELOP_IMAGE_VERSION"

          # -------------------------------
          # Release branch
          # -------------------------------
          elif [[ "$BRANCH_NAME" == release/* ]]; then
              IMAGE_VERSION="${BRANCH_NAME#release/}"
              echo "Release version extracted: $IMAGE_VERSION"

              if ! validate_version "$IMAGE_VERSION"; then
                  echo "❌ Invalid semantic version: $IMAGE_VERSION"
                  STOP_PIPELINE=true
                  SKIP_CD=true
                  exit 1
              fi

              if [[ "$BRANCH_NAME" != "$RELEASE_BRANCH" && "$(Build.Reason)" == "Manual" ]]; then
                  echo "Manual build with release mismatch – skipping CD"
                  SKIP_CD=true
              elif [[ "$BRANCH_NAME" != "$RELEASE_BRANCH" ]]; then
                  echo "Release mismatch – failing pipeline"
                  STOP_PIPELINE=true
                  SKIP_CD=true
                  exit 1
              fi
          else
              echo "Invalid branch detected"
              STOP_PIPELINE=true
              SKIP_CD=true
              exit 1
          fi

          echo "Final image version: $IMAGE_VERSION"

          echo "##vso[task.setvariable variable=IMAGE_VERSION;isOutput=true]$IMAGE_VERSION"
          echo "##vso[task.setvariable variable=BRANCH_NAME;isOutput=true]$BRANCH_NAME"
          echo "##vso[task.setvariable variable=STOP_PIPELINE;isOutput=true]$STOP_PIPELINE"
          echo "##vso[task.setvariable variable=SKIP_CD;isOutput=true]$SKIP_CD"

        name: CheckBranch
        displayName: 'Branch & Version Validation'
        continueOnError: true
        env:
          RELEASE_BRANCH: $(sprint_release_branch)
          DEVELOP_IMAGE_VERSION: $(develop_image_version)

  # ------------------------------------------------
  # Job 2: Docker Build & Push
  # ------------------------------------------------
  - job: Docker_Build_Push
    displayName: 'Docker Build & Push'
    dependsOn: Branch_Check
    pool: 'hsn-shost-agent'
    condition: and(
      succeeded(),
      eq(dependencies.Branch_Check.outputs['CheckBranch.STOP_PIPELINE'], 'false'),
      eq(dependencies.Branch_Check.outputs['CheckBranch.SKIP_CD'], 'false')
    )
    variables:
      IMAGE_VERSION: $[ dependencies.Branch_Check.outputs['CheckBranch.IMAGE_VERSION'] ]
      BRANCH_NAME: $[ dependencies.Branch_Check.outputs['CheckBranch.BRANCH_NAME'] ]
      GIT_COMMIT: $(Build.SourceVersion)
      BUILD_TIME: $[format('{0:yyyy-MM-ddTHH:mm:ssZ}', pipeline.startTime)]
      PROJECT_NAME: $(Build.Repository.Name)

    steps:
      - bash: |
          set -e

          echo "Logging into Docker Hub"
          echo "$(DOCKER_PAT)" | docker login -u "$(DOCKER_USERNAME)" --password-stdin

          docker buildx create --name facility-erp-builder --use || true
          docker buildx inspect --bootstrap

          echo "Building Backend Image"
          docker buildx build \
            --platform $(PLATFORM) \
            --tag $(DOCKER_USERNAME)/facility-erp-backend:$(IMAGE_VERSION) \
            --label org.opencontainers.image.source="$(PROJECT_NAME)" \
            --label org.opencontainers.image.revision="$(GIT_COMMIT)" \
            --label org.opencontainers.image.created="$(BUILD_TIME)" \
            --label org.opencontainers.image.version="$(IMAGE_VERSION)" \
            --label org.opencontainers.image.ref.name="$(BRANCH_NAME)" \
            --load \
            -f apps/backend/Dockerfile \
            apps/backend

          echo "Building Frontend Image"
          docker buildx build \
            --platform $(PLATFORM) \
            --tag $(DOCKER_USERNAME)/facility-erp-frontend:$(IMAGE_VERSION) \
            --label org.opencontainers.image.source="$(PROJECT_NAME)" \
            --label org.opencontainers.image.revision="$(GIT_COMMIT)" \
            --label org.opencontainers.image.created="$(BUILD_TIME)" \
            --label org.opencontainers.image.version="$(IMAGE_VERSION)" \
            --label org.opencontainers.image.ref.name="$(BRANCH_NAME)" \
            --load \
            -f apps/frontend/Dockerfile \
            apps/frontend

          docker push $(DOCKER_USERNAME)/facility-erp-backend:$(IMAGE_VERSION)
          docker push $(DOCKER_USERNAME)/facility-erp-frontend:$(IMAGE_VERSION)

          echo "Docker images pushed successfully"

        displayName: 'Docker Build & Push'
        env:
          DOCKER_PAT: $(docker_pat)
          DOCKER_USERNAME: $(docker_username)
